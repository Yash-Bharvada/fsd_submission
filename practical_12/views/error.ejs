<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Oops! - Kids Calculator</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .error-container {
            text-align: center;
            padding: 50px 20px;
            max-width: 600px;
            margin: 0 auto;
        }
        
        .error-icon {
            font-size: 5rem;
            color: #ff6b6b;
            margin-bottom: 20px;
            animation: bounce 2s infinite;
        }
        
        .error-title {
            font-size: 2.5rem;
            color: #4a5568;
            margin-bottom: 15px;
        }
        
        .error-message {
            font-size: 1.2rem;
            color: #718096;
            margin-bottom: 30px;
            line-height: 1.6;
        }
        
        .error-actions {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }
        
        .error-btn {
            padding: 15px 30px;
            border: none;
            border-radius: 15px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            font-family: inherit;
        }
        
        .primary-btn {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }
        
        .primary-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(102, 126, 234, 0.4);
        }
        
        .secondary-btn {
            background: linear-gradient(135deg, #4facfe, #00f2fe);
            color: white;
            box-shadow: 0 8px 25px rgba(79, 172, 254, 0.3);
        }
        
        .secondary-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 35px rgba(79, 172, 254, 0.4);
        }
        
        .fun-message {
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
            padding: 20px;
            border-radius: 15px;
            margin-top: 30px;
            font-weight: 600;
            color: #4a5568;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="error-container">
            <div class="error-icon">
                <% if (status === 404) { %>
                    <i class="fas fa-search"></i>
                <% } else { %>
                    <i class="fas fa-exclamation-triangle"></i>
                <% } %>
            </div>
            
            <h1 class="error-title">
                <% if (status === 404) { %>
                    Oops! Page Not Found
                <% } else { %>
                    Something Went Wrong
                <% } %>
            </h1>
            
            <div class="error-message">
                <% if (status === 404) { %>
                    <p>🔍 We couldn't find the page you're looking for!</p>
                    <p>Don't worry, it happens to the best of us. Let's get you back to calculating!</p>
                <% } else { %>
                    <p>😅 Oops! Something unexpected happened.</p>
                    <p>Our math wizards are working to fix it. Let's try again!</p>
                <% } %>
            </div>
            
            <div class="error-actions">
                <a href="/" class="error-btn primary-btn">
                    <i class="fas fa-calculator"></i>
                    Back to Calculator
                </a>
                
                <a href="/health" class="error-btn secondary-btn">
                    <i class="fas fa-heartbeat"></i>
                    Check Status
                </a>
            </div>
            
            <div class="fun-message">
                <i class="fas fa-lightbulb"></i>
                <strong>Fun Fact:</strong> Even the greatest mathematicians make mistakes sometimes! 
                That's how we learn and get better at math! 🌟
            </div>
        </div>
    </div>
    
    <script>
        // Add some fun interactions
        document.querySelector('.error-icon').addEventListener('click', function() {
            this.style.animation = 'bounce 0.6s ease';
            setTimeout(() => {
                this.style.animation = 'bounce 2s infinite';
            }, 600);
        });
        
        // Auto-redirect after 10 seconds for 404 errors
        <% if (status === 404) { %>
            let countdown = 10;
            const countdownElement = document.createElement('p');
            countdownElement.style.cssText = 'margin-top: 20px; color: #718096; font-size: 0.9rem;';
            countdownElement.innerHTML = `Redirecting to calculator in ${countdown} seconds...`;
            document.querySelector('.error-message').appendChild(countdownElement);
            
            const timer = setInterval(() => {
                countdown--;
                countdownElement.innerHTML = `Redirecting to calculator in ${countdown} seconds...`;
                
                if (countdown <= 0) {
                    clearInterval(timer);
                    window.location.href = '/';
                }
            }, 1000);
        <% } %>
    </script>
</body>
</html>